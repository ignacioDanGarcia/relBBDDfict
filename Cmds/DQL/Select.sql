SELECT id, nombre, salario FROM templeados;

SELECT * FROM templeados;

SELECT id, nombre, salario
FROM templeados
WHERE salario > 5000;

SELECT id, nombre, salario
FROM templeados
ORDER BY salario DESC;